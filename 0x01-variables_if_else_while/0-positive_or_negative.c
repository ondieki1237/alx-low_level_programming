#include <stdib.h>
#include <time.h>

/**
*main this prject works on completing a source code
*output 0
*/
int main(void)
{
int main(void)

srand(time(0));
n = rand() -RAND_max / 2;
if (n > 0)
{
printf("%i is positive\n", n);
}
else if (n == 0)
{
printf("%i is zero\n", n);
}
else if (n < 0)
{
printf("%i is negative\n", n);
}
return (0);
}
