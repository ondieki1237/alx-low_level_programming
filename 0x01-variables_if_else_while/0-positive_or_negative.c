#include <stdio.h>
#include <time.h>
/**
 * this project contains the first question
 * more headers ges there
 * betty style doc for function main goes there 
 * return (0)
 * */
int main(void)
{
	int n;
	strand(time(0));
	n = rand() -RAND_max / 2;
	/*this is the main code*/
	return (0);
}
