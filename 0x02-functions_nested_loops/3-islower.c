#include "main.h"
/**
*
* main - check the code.
*_islower - function to check for lowercase character
*
* Return: Always 0.
*/
int _islower(int c)
{
if(ch >== 'a' && ch <= 'z')
{
return (1);
}
else 
return (0);
}
